@model  FAPP.ViewModel.Common.GraphsModel
<div class="box box-color box-bordered green" id="ShowTopAttendanceCheckIns">
    <div class="box-title">
        <h3>
            <i class="fa fa-bars"></i>
            <span id="LabelTitle">Latest Check-Ins</span>
        </h3>
    </div>  
    <div class="box-content" style="min-height: 155px;">
        @if (Model != null)
        {
            @*@Html.Raw(Model)*@
        }
    </div>
</div>
